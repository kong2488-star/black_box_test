# 이 파일을 복사해서 .env 로 만들고 값을 채운다.
#   cp .env.example .env
#
# .env 는 .gitignore 로 막혀 있다. 이 예시 파일만 저장소에 들어간다.
# 실제 키를 이 파일(.env.example)에 적지 말 것 — 이건 추적된다.

# Google AI Studio 에서 발급: https://aistudio.google.com/apikey
GEMINI_API_KEY=
